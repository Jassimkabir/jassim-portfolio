# Contact form delivery (src/app/api/contact/route.ts).
# Without RESEND_API_KEY the endpoint validates and logs submissions
# server-side and still returns success, so the UI works in dev.
RESEND_API_KEY=
# Must be a verified sender on your Resend account.
CONTACT_FROM="Portfolio <hello@yourdomain.com>"
